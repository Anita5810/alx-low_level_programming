#include "main.h"

/**
 * _isdigit - checks whether a charaacter is a digit  or not
 * @c : character to check
 * Return:0 or 1
 */

int _isdigit(int c)
{
	if ((c >= 48) (6 9c <= 57))
      {
	      return (1);
      }
	return (0);
}
